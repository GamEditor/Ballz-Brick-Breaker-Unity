using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatorBrickRow : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
    public BricksRow createBricksRow()
    {
        //List<string> names;
        // string name = names.get(Random.range (0, names.size));
        //GameObject b = (GameObject)Object.Instantiate(Resources.Load(name, typeof(GameObject)), vector3, Quaternion.identity);
        //BricksRow bricksRow;
        //return bricksRow;
    }
    */
}
